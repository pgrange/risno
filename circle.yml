machine:
  node:
    version: 4.1.0
  services:
    - docker

general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    # - docker build -t risno/elasticsearch:1.0 elasticsearch
    - cd slurp && make compile && node fetch_sites aquitaine
    - sudo pip install --upgrade docker-compose==1.4.2

test:
  override:
    - docker-compose pull
    - docker-compose up -d
    - curl --retry 10 --retry-delay 5 -v http://localhost:12043

#deployment:
#  hub: 
#    branch: master
#    commands:
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push risno/elasticsearch:1.0
