#
# Risno configuration
#

RISNO_DIR="/var/opt/risno"
cat /etc/opt/risno.json
{
 "listen_port": 80,
 "smtp": {
  "host": "smtp.host",
  "auth": {
   "user": "contact@risno.org",
   "pass": "none"
   }
 }
}
